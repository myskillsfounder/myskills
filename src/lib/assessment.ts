/**
 * DEPRECATED (2026-07-11) — superseded by lib/assessmentResults.ts, which
 * reads/writes the normalized `initial_assessment_results` /
 * `initial_assessment_category_scores` tables instead of the old
 * `profiles.assessment` JSONB column. Nothing imports this file anymore.
 * Left in place (rather than deleted) only because this sandbox's mounted
 * drive can't reliably delete files — safe to remove by hand later.
 */
