// Retired 2026-07-11 — its skill-coverage list duplicated the track cards and
// confused the page. Progress now lives in PracticeOverview (summary) + the
// track cards (per-track). Kept as an inert stub because the mounted drive
// can't reliably delete files.
export {}
