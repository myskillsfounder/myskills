// Retired — the profile "About" section was removed from the product and the
// `profiles.about` column dropped. Kept as an inert stub because this drive
// can't reliably delete files; safe to delete from the repo.
export {}
